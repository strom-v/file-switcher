/**
 * Общие константы для main, preload и renderer.
 */

/** Порт локального прокси по умолчанию — при первом запуске и как fallback, если сохранённого нет */
export const DEFAULT_PROXY_PORT = 38765
